# Godot 4+ specific ignores
.godot/
/android/

**.tmp

# Godot-specific files
.import/                 # Import cache (generated for assets like images and sounds)
export_presets.cfg       # Export presets, which often contain machine-specific paths

# Godot Engine project files
*.mono/                  # Mono (C#) build cache
data.mdb                 # Mono database
*.log                    # Log files (optional)
*.pdb                    # Mono debugging symbols
*.builds                 # Optional Godot export build files

# System-specific files
.DS_Store                # macOS metadata
Thumbs.db                # Windows thumbnail database
desktop.ini              # Windows desktop file
.idea/                   # JetBrains IDEs
.v
